# Public site URL — used for canonical links, Open Graph, sitemap, and structured data.
NEXT_PUBLIC_SITE_URL=https://www.sunsetcounselingcenter.com

# Appointment inquiry delivery — configure ONE option.
# Without either, inquiries are logged to the console in development and
# visitors are asked to call the office in production.

# Option A: email via Resend (https://resend.com)
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL="Sunset Counseling Center Website <website@your-verified-domain.com>"

# Option B: POST JSON to a form service or CRM webhook
CONTACT_WEBHOOK_URL=
