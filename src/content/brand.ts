/**
 * Brand artwork, derived from public/sunsetlogo.png (the original, untouched).
 *
 *   logo-full   — the complete stacked logo, trimmed
 *   logo-mark   — the sunburst only
 *   logo-script — the "Sunset" script only
 *
 * To update the logo, replace public/sunsetlogo.png and regenerate these crops
 * (see "Brand assets" in the README).
 */
import logoFull from "../../public/brand/logo-full.png";
import logoMark from "../../public/brand/logo-mark.png";
import logoScript from "../../public/brand/logo-script.png";

export const brand = { logoFull, logoMark, logoScript };
